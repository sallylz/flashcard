Run "node createDB.js" to first create the database.
Run "node translateServer.js" in the directory "flashcards".
Review Policy: App iterates through the database by taking the least seen vocabulary.
